type Query {
    hello: String!
    articles: [Article!]
    article(id: String!): Article!
}

type Mutation {
  deleteArticles(id: ID!): ID!
  createArticles(input: CreateArticleInput!): Article!
  updateArticles(id: ID! input: UpdateArticleInput): Article!
}

type Article {
    _id: ID!
    title: String!
    author: String!
    content: String!
    isBlocked: Boolean!
    categories: String!
    comments: [Comments!]!
}

type Comments {
    _id: ID!
    content: String!
    user: String!
}

input CreateArticleInput {
    title: String!
    author: String!
    content: String!
}

input UpdateArticleInput {
    title: String!
    author: String!
    content: String!
    isBlocked: Boolean!
}